import React from 'react';
import './UserHeader.css';

const UserHeader: React.FC = () => {
    return (
        <div>
            <h2>UserHeader</h2>

        </div>
    );
};

export default UserHeader;